package simpleml.model.supervised

import simpleml.data.Dataset

open class Estimator {
	open fun fit(features: Dataset, target: Dataset) -> trainedModel: Model
}

interface Model {
	fun predict(features: Dataset) -> results: Any
}

// Interfaces come later

