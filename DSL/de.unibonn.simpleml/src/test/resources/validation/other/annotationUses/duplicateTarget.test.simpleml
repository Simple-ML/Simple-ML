package tests.validation.other.annotationUses.duplicateTarget

@Target(
    // semantic_warning "This annotation target is used twice."
    »AnnotationTarget.Annotation«,
    // semantic_warning "This annotation target is used twice."
    »AnnotationTarget.Annotation«,
    // no_semantic_warning "This annotation target is used twice."
    »AnnotationTarget.Class«
)
annotation TestAnnotation
