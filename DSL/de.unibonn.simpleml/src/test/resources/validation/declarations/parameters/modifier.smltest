package tests.validation.declarations.parameters.modifiers

// semantic_error "A parameter must have no modifiers."
// semantic_error "A parameter must have no modifiers."
// semantic_error "A parameter must have no modifiers."
annotation MyAnnotation(»open« »override« »static« a: Int)

// semantic_error "A parameter must have no modifiers."
// semantic_error "A parameter must have no modifiers."
// semantic_error "A parameter must have no modifiers."
class MyClass(»open« »override« »static« a: Int) {

    // semantic_error "A parameter must have no modifiers."
    // semantic_error "A parameter must have no modifiers."
    // semantic_error "A parameter must have no modifiers."
    class MyClass(»open« »override« »static« a: Int)

    // semantic_error "A parameter must have no modifiers."
    // semantic_error "A parameter must have no modifiers."
    // semantic_error "A parameter must have no modifiers."
    fun myFunction(»open« »override« »static« a: Int)
}

// semantic_error "A parameter must have no modifiers."
// semantic_error "A parameter must have no modifiers."
// semantic_error "A parameter must have no modifiers."
fun myFunction(»open« »override« »static« a: Int)

// semantic_error "A parameter must have no modifiers."
// semantic_error "A parameter must have no modifiers."
// semantic_error "A parameter must have no modifiers."
step myStep1(»open« »override« »static« a: Int) {}

// semantic_error "A parameter must have no modifiers."
// semantic_error "A parameter must have no modifiers."
// semantic_error "A parameter must have no modifiers."
step myStep2(
    f: callable (»open« »override« »static« a: Int) -> ()
) {}
