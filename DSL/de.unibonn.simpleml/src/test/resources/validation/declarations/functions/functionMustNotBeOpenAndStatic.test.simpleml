package tests.validation.declarations.functions.functionMustNotBeOpenAndStatic

class MyClass {
    // semantic_error "A function must not be static and open."
    open static fun »openStaticFunction«()
    // no_semantic_error "A function must not be static and open."
    open fun »openFunction«()
    // no_semantic_error "A function must not be static and open."
    static fun »staticFunction«()
}
