package simpleml.model

import simpleml.dataset.Dataset

open class Estimator {
    open fun fit(features: Dataset, target: Dataset) -> trainedModel: Model
}

open class Model {
    open fun predict(features: Dataset) -> results: Any
}
