package tests.validation.declarations.functions.modifiers

// semantic_error "A top-level function must have no modifiers."
// semantic_error "A top-level function must have no modifiers."
// semantic_error "A top-level function must have no modifiers."
»open« »override« »static« fun myFunction1()

class MyClass {
    // no_semantic_error
    // no_semantic_error
    // no_semantic_error
    »open« »override« »static« fun myFunction()
}
