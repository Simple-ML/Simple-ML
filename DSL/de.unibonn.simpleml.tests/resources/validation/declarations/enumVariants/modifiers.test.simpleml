package tests.validation.declarations.enumVariants.modifiers

enum MyEnum {

    // semantic_error "An enum variant must have no modifiers."
    // semantic_error "An enum variant must have no modifiers."
    // semantic_error "An enum variant must have no modifiers."
    »open« »override« »static« MyEnumVariant1
}
