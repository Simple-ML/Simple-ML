package tests.modifiers

// semantic_error "A type parameter must have no modifiers."
// semantic_error "A type parameter must have no modifiers."
// semantic_error "A type parameter must have no modifiers."
// semantic_error "A type parameter must have no modifiers."
// semantic_error "A type parameter must have no modifiers."
class MyClass<»deprecated« »open« »override« »pure« »static« T> {

    // semantic_error "A type parameter must have no modifiers."
    // semantic_error "A type parameter must have no modifiers."
    // semantic_error "A type parameter must have no modifiers."
    // semantic_error "A type parameter must have no modifiers."
    // semantic_error "A type parameter must have no modifiers."
    class MyClass<»deprecated« »open« »override« »pure« »static« T>

    // semantic_error "A type parameter must have no modifiers."
    // semantic_error "A type parameter must have no modifiers."
    // semantic_error "A type parameter must have no modifiers."
    // semantic_error "A type parameter must have no modifiers."
    // semantic_error "A type parameter must have no modifiers."
    fun myFunction<»deprecated« »open« »override« »pure« »static« T>()
}

// semantic_error "A type parameter must have no modifiers."
// semantic_error "A type parameter must have no modifiers."
// semantic_error "A type parameter must have no modifiers."
// semantic_error "A type parameter must have no modifiers."
// semantic_error "A type parameter must have no modifiers."
fun myFunction<»deprecated« »open« »override« »pure« »static« T>()
