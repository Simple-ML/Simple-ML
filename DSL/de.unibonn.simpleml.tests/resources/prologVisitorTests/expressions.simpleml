package de.unibonn.test_package

fun callWithoutReceiver(argument: Int)

workflow test {
    val reference = 0;

    // Argument, ProcessCall and Receiver
    callWithoutReceiver(argument = reference);
    callReceiver.callWithReceiver();

    // AttributeAccess
    attributeAccessReceiver.attributeAccess;

    // IndexedAccess
    indexedAccess[0];

    // Operators
    val infix = 1 + 2;
    val prefix = -1;
}