package myPackage

step myFunctionalStep() -> a {

    // Assignment with placeholder, wildcard, and yield
    (val myPlaceholder, _, yield a) = f();

    // Expression statement
    lambda () {

        // Lambda yield
        yield myLambdaYield = 1;
    };
}

step myStepWithUnresolvedYield() {
    yield a = f();
}
