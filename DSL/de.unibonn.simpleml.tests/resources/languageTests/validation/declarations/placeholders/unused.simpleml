step myFunction() {
    call(unused);

    // semantic_warning "This placeholder is unused."
    val »unused« = 1;

    // no_semantic_warning "This placeholder is unused."
    val »used« = 1;
    call(used);
}

workflow myWorkflow {
    call(unused);

    // semantic_warning "This placeholder is unused."
    val »unused« = 1;

    // no_semantic_warning "This placeholder is unused."
    val »used« = 1;
    call(used);
}