package tests.overridingFunctionMustOverrideSomething

open class MyOpenClass {
    open fun classFunction()
}

interface MyInterface {
    fun interfaceFunction()
}

class MyClass sub MyOpenClass, MyInterface {
    // semantic_error "This function does not override anything."
    override fun »ownFunction«()

    // no_semantic_error "This function does not override anything."
    override fun »classFunction«()

    // no_semantic_error "This function does not override anything."
    override fun »interfaceFunction«()
}