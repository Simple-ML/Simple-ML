package tests.functionMustNotBeOverrideAndStatic

class MyClass {
    // semantic_error "A function must not be static and override."
    override static fun »overrideStaticFunction«()
    // no_semantic_error "A function must not be static and override."
    override fun »overrideFunction«()
    // no_semantic_error "A function must not be static and override."
    static fun »staticFunction«()
}

interface MyInterface {
    // no_semantic_error "A function must not be static and override."
    override static fun »openStaticFunction«()
}