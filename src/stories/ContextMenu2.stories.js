import React from 'react';
import  ContextMenu from './ContextMenu'
export default {
  title: 'ContextMenu/Select',
  component: ContextMenu,
};
const Template = (args) => <ContextMenu {...args} />;

export const Select_Data_set = Template.bind({});
