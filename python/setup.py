from setuptools import setup

if __name__ == "__main__":
    setup(
        name='Simple-ML',
        version='0.0.1',
        description='Adapters for various ML libraries',
        packages=['simpleml']
    )