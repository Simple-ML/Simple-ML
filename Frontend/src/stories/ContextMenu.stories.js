import React from 'react';
import  ContextMenu from './ContextMenu'
export default {
  title: 'ContextMenu',
  component: ContextMenu,
};
const Template = (args) => <ContextMenu {...args} />;

export const Add_Data = Template.bind({});
